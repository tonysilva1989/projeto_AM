function z = v_func(x,v,P,n,@gaussian_kernel)
	%n: tamanho do vetor x de atributos
	
	lenght_P = size(P); %tamanho do vetor de partições escolhido
	sum = 0;
	for i=1:lenght_P
		for j=1:n
		
		end
	
		
		
		sum = sum + gaussian_kernel()*
	end
end