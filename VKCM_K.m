function out = VKCM_K()
    %argumentos de entrada: 
        %conjunto de dados de entrada X = {x1,...xn}
        %número de Clusters K=3
    p=3; %tamanho p (número de atributos)
    %Inicialização:
    %definindo os valores iniciais do vetor de pesos lambda
    lambda = ones([1,p]);
end